import React, { Component } from 'react';
import Memos from './../Memos'

class Main extends Component {
  render() {
    return (
      <main className={'main'}>
        <Memos />
      </main>);
  }
}

export default Main;
